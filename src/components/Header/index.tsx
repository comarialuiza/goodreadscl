import React from 'react';

import { Container } from './styles';

const Header: React.FC = () => {
    return(
        <Container>
            This is the header!
        </Container>
    );
}

export default Header;